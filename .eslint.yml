{
    "extends": "eslint:recommended",
    "env": {
        "node": true,
        "mocha": true,
        "es6": true
    },
    "rules": {
        "no-var": "warn",
        "no-console": "warn",
        "no-unused-vars": "warn",
        "no-useless-escape": "warn"
    }
}